<%= simple_form_for [category, item] do |f| %>
  <%= f.association :category, label: 'Bookmark category', class: 'add-text' %>
  <%= f.input :item_url, label: 'Bookmark link', class: 'add-text' %>
  <%= f.submit 'Create bookmark', class: 'btn btn-primary', id: 'add-update-item' %>
<% end %>
